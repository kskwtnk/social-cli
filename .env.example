# Bluesky認証情報
# App Passwordの取得方法:
# 1. Blueskyアプリまたはウェブにログイン
# 2. Settings → Privacy and Security → App Passwords
# 3. "Add App Password" をクリック
# 4. 名前を入力（例: "social-cli"）
# 5. 生成されたパスワードをコピーして下記に貼り付け

BLUESKY_IDENTIFIER=your-handle.bsky.social
BLUESKY_APP_PASSWORD=xxxx-xxxx-xxxx-xxxx

# X (Twitter) 認証情報
# OAuth 1.0a認証キーの取得方法:
# 1. X Developer Portal (https://developer.x.com/) にアクセス
# 2. プロジェクトとアプリを作成
# 3. App settings → Keys and tokens
# 4. Consumer Keys (API Key and Secret) を生成
# 5. Authentication Tokens (Access Token and Secret) を生成
# 6. 各値をコピーして下記に貼り付け

X_CONSUMER_KEY=your_consumer_key_here
X_CONSUMER_SECRET=your_consumer_secret_here
X_ACCESS_TOKEN=your_access_token_here
X_ACCESS_TOKEN_SECRET=your_access_token_secret_here

# Threads 認証情報
# Access Tokenの取得方法:
#
# 1. Meta for Developers (https://developers.facebook.com/) にアクセス
# 2. 「アプリを作成」をクリック
# 3. アプリタイプ選択画面で「その他」または「ビジネス」を選択
# 4. アプリ名とメールアドレスを入力
# 5. ビジネスポートフォリオ選択画面で「現時点ではビジネスポートフォリオをリンクしない」を選択
# 6. アプリが作成されたら、左メニューの「製品を追加」から「Threads」を探して「設定」をクリック
# 7. Threads製品の設定画面でAccess Tokenを生成
#    - 必要な権限: threads_basic, threads_content_publish
#
# User IDの取得方法:
# 8. ターミナルで以下のコマンドを実行（YOUR_ACCESS_TOKENは手順7で取得したトークン）:
#    curl "https://graph.threads.net/v1.0/me?fields=id,username&access_token=YOUR_ACCESS_TOKEN"
# 9. レスポンスのJSONから "id" フィールドの値（数値）をコピー
#    例: {"id":"33034957766151404","username":"kskwtnk"} → 33034957766151404 をコピー
# 10. 取得した数値IDをTHREADS_USER_IDに、Access TokenをTHREADS_ACCESS_TOKENに設定

THREADS_USER_ID=your_user_id_here
THREADS_ACCESS_TOKEN=your_access_token_here

# 注意:
# - 通常のパスワードではなく、App Passwordを使用してください（Bluesky）
# - X (Twitter) はOAuth 1.0aの4つのキーが必要です
# - Threads はOAuth 2.0のUser ID（数値）とAccess Tokenが必要です
#   ※ User IDはユーザー名（@kskwtnk）ではなく、数値ID（例: 33034957766151404）を使用
# - このファイルをコピーして .env を作成してください: cp .env.example .env
# - .env ファイルは .gitignore に含まれており、Gitにコミットされません
